# Example prompts

- Connect to Resolve, summarize the current project and timeline, then list the
  clips on V1. Do not change anything.
- Add a blue marker named "Client note" at frame 240 with the note "Review cut".
- Inspect the node tree of clip 3 on video track 1 and report labels, LUTs, and
  bypass-relevant state.
- Copy the grade from V1 clip 2 to V1 clips 3, 4, and 5.
- Find registered bridal PowerGrades, apply `luxury_bridal` to V1 clip 6, and
  report exactly what changed.
- Configure the saved preset "YouTube 1080p", add the current timeline to the
  render queue at `/Volumes/Exports`, then show the returned job ID. Do not start.
- Monitor render job `<job-id>` and report completion percentage and status.
